using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEditor.Rendering.Universal;
using UnityEngine;
using UnityEngine.UIElements;

public class CameraFollow : MonoBehaviour
{

   /* public GameObject Ball;
    private Transform BallPosition;
    private Transform CurrentPosition;
    private Vector3 CurrentVelocity = Vector3.zero;
    private float SmooothTime = 0.25f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
        BallPosition = Ball.transform;
        CurrentPosition = transform;

        Distance(BallPosition.position, CurrentPosition.position);
    }
    s
    void Distance(Vector3 BallPosition, Vector3 CurrentPosition)
    {
        Vector3 Interval = BallPosition - CurrentPosition;
        transform.position = BallPosition + Interval;
    }*/
}
